import { SUBSTRATES } from '../components/products/substrates/SubstrateData';

export const Substrates = (state=SUBSTRATES,action) =>{
    switch(action.type){
        default:
            return state;
    }
}